meta {
  name: Send Plain Text Webhook
  type: http
  seq: 4
}

post {
  url: {{BASE_URL}}/webhook/esl/{{CUSTOMER_ID}}
  body: text
  auth: bearer
}

headers {
  Content-Type: text/plain
}

auth:bearer {
  token: {{API_TOKEN}}
}

body:text {
  This is a plain text webhook payload.
  It can contain any text content.
  Line 3 of the message.
}
